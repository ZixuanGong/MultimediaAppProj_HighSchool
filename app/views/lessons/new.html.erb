<div class="span6 offset3">
	<h1>New Lesson</h1>

	<%= form_for(@lesson) do |f| %>
	   	<%= render 'fields', f: f%>

		<%= f.submit "Create Lesson", class: "btn btn-large btn-primary" %>
	<% end %>
</div>
