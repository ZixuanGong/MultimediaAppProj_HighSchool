<div class="span6 offset3">
	<h1>New Course</h1>

	<%= form_for(@course) do |f| %>
	   	<%= render 'fields', f: f%>

		<%= f.submit "Create Course", class: "btn btn-large btn-primary" %>
	<% end %>
</div>
